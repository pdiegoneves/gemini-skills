---
name: start-django-project
description: Use essa skill sempre que precisar criar um projeto django
---

# Instruções de Revisão
1. Crie inicie e o projeto com o uv usando o comando `uv init`
2. Adicione o django usando o comando `uv add django`
3. Crie o projeto django com o comando `uv run django-admin startproject core .`